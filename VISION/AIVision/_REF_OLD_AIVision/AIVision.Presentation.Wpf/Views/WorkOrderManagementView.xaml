<Window x:Class="AIVision.Presentation.Wpf.Views.WorkOrderManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AIVision.Presentation.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="工單管理"
        Width="1200"
        Height="700"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundGrayBrush}"
        d:DataContext="{d:DesignInstance vm:WorkOrderManagementViewModel, IsDesignTimeCreatable=False}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 標題列 -->
        <TextBlock Grid.Row="0"
                   Text="工單管理"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,16" />

        <!-- 篩選與操作列 -->
        <Border Grid.Row="1"
                CornerRadius="8"
                Background="White"
                Padding="16"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- 篩選 -->
                <TextBlock Grid.Column="0"
                           Text="狀態篩選："
                           VerticalAlignment="Center"
                           Margin="0,0,8,0" />

                <ComboBox Grid.Column="1"
                          Width="120"
                          SelectedValue="{Binding FilterStatus, Mode=TwoWay}"
                          Margin="0,0,12,0">
                    <ComboBoxItem Content="全部" IsSelected="True" />
                    <ComboBoxItem Content="進行中" />
                    <ComboBoxItem Content="已結束" />
                </ComboBox>

                <Button Grid.Column="2"
                        Content="套用篩選"
                        Command="{Binding ApplyFilterCommand}"
                        Style="{StaticResource ToolbarButton}"
                        Margin="0,0,12,0" />

                <!-- 操作按鈕 -->
                <StackPanel Grid.Column="4"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right">
                    <Button Content="重新載入"
                            Command="{Binding LoadWorkOrdersCommand}"
                            Style="{StaticResource ToolbarButton}"
                            Margin="0,0,8,0" />
                    <Button Content="新增工單"
                            Command="{Binding CreateNewWorkOrderCommand}"
                            Background="#FF4CAF50"
                            Foreground="White"
                            Padding="12,8"
                            BorderThickness="0"
                            Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="6"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                  VerticalAlignment="Center" />
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#FF45A049" />
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 工單列表 -->
        <Border Grid.Row="2"
                CornerRadius="8"
                Background="White"
                Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- 列表標題 -->
                <TextBlock Grid.Row="0"
                           Text="工單列表"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,12" />

                <!-- DataGrid -->
                <DataGrid Grid.Row="1"
                          ItemsSource="{Binding WorkOrders}"
                          SelectedItem="{Binding SelectedWorkOrder, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#FFF9F9F9"
                          RowHeight="40">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="工單代碼"
                                            Binding="{Binding Code}"
                                            Width="180" />
                        <DataGridTextColumn Header="產品名稱"
                                            Binding="{Binding ProductName}"
                                            Width="150" />
                        <DataGridTextColumn Header="模型"
                                            Binding="{Binding ModelName}"
                                            Width="120" />
                        <DataGridTextColumn Header="狀態"
                                            Binding="{Binding StatusDisplay}"
                                            Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#FF666666" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Status}" Value="Active">
                                            <Setter Property="Foreground" Value="#FF4CAF50" />
                                            <Setter Property="FontWeight" Value="Bold" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="開始時間"
                                            Binding="{Binding StartAt, StringFormat=yyyy/MM/dd HH:mm}"
                                            Width="140" />
                        <DataGridTextColumn Header="持續時間"
                                            Binding="{Binding DurationDisplay}"
                                            Width="100" />
                        <DataGridTextColumn Header="總數"
                                            Binding="{Binding TotalCount}"
                                            Width="60">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="NG"
                                            Binding="{Binding NgCount}"
                                            Width="60">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right" />
                                    <Setter Property="Foreground" Value="#FFF44336" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="良率"
                                            Binding="{Binding YieldPercentage, StringFormat={}{0:F1}%}"
                                            Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTemplateColumn Header="操作" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="切換"
                                                Command="{Binding DataContext.SwitchToWorkOrderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                Background="#FF2196F3"
                                                Foreground="White"
                                                Padding="8,4"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                Margin="4,0">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        CornerRadius="4"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                                      VerticalAlignment="Center" />
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="#FF1976D2" />
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                        <Button Content="刪除"
                                                Command="{Binding DataContext.DeleteWorkOrderCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                Background="#FFF44336"
                                                Foreground="White"
                                                Padding="8,4"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                Margin="4,0">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}"
                                                                        CornerRadius="4"
                                                                        Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                                      VerticalAlignment="Center" />
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter Property="Background" Value="#FFD32F2F" />
                                                                    </Trigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- 載入中提示 -->
                <Grid Grid.Row="1"
                      Background="#80FFFFFF"
                      Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                        <TextBlock Text="載入中..."
                                   FontSize="16"
                                   Foreground="#FF666666" />
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- 狀態列 -->
        <Border Grid.Row="3"
                CornerRadius="8"
                Background="White"
                Padding="12"
                Margin="0,16,0,0">
            <TextBlock Text="{Binding StatusMessage}"
                       FontSize="12"
                       Foreground="#FF666666" />
        </Border>
    </Grid>
</Window>
