<Window x:Class="AIVision.Presentation.Wpf.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace=AIVision.Presentation.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="使用者登入"
        Width="420"
        Height="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="White"
        d:DataContext="{d:DesignInstance vm:LoginViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="AIVision 登入"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,24" />

        <StackPanel Grid.Row="1">
            <TextBlock Text="帳號" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,6,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,16,0,0">
            <TextBlock Text="密碼" />
            <PasswordBox x:Name="PasswordBox" PasswordChanged="OnPasswordChanged" Margin="0,6,0,0" />
        </StackPanel>

        <!-- 錯誤訊息 -->
        <TextBlock Grid.Row="3"
                   Text="{Binding ErrorMessage}"
                   Foreground="#FFE53935"
                   FontSize="13"
                   Margin="0,12,0,0"
                   Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="取消" Width="100" Command="{Binding CancelCommand}" />
            <Button Content="登入" Width="100" IsDefault="True" Command="{Binding LoginCommand}" Margin="12,0,0,0" />
        </StackPanel>
    </Grid>
</Window>
