<Window x:Class="AIVision.Presentation.Wpf.Views.CameraView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace=AIVision.Presentation.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="相機面板"
        Width="960"
        Height="680"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundGrayBrush}"
        d:DataContext="{d:DesignInstance vm:CameraViewModel, IsDesignTimeCreatable=True}">
    <Grid Margin="24" Loaded="OnLoaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="相機：" VerticalAlignment="Center" FontSize="16" />
            <ComboBox Width="360"
                      ItemsSource="{Binding AvailableCameras}"
                      SelectedItem="{Binding SelectedCamera}"
                      DisplayMemberPath="Display"
                      Margin="12,0,0,0" />
            <Button Content="重新掃描"
                    Command="{Binding RefreshDevicesCommand}"
                    Style="{StaticResource ToolbarButton}"
                    Margin="12,0,0,0" />
        </StackPanel>

        <Border Grid.Row="1"
                Background="#FF101010"
                CornerRadius="12"
                Padding="16"
                Margin="0,16">
            <Grid>
                <Image Source="{Binding FrameBitmap}"
                       Stretch="Uniform">
                    <Image.Style>
                        <Style TargetType="Image">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding FrameBitmap}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>
                <TextBlock Text="尚未取得影像"
                           Foreground="#FFCCCCCC"
                           FontSize="18"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding FrameBitmap}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                Background="#FF1F1F1F"
                CornerRadius="12"
                Padding="12"
                Margin="0,8,0,16">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasCameraParameters}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <ItemsControl ItemsSource="{Binding CameraParameters}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,6">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0"
                                       Text="{Binding DisplayName}"
                                       FontSize="14"
                                       Foreground="#FFEEEEEE"
                                       VerticalAlignment="Center"
                                       ToolTip="{Binding Tooltip}" />

                            <Slider Grid.Column="1"
                                    Minimum="{Binding Minimum}"
                                    Maximum="{Binding Maximum}"
                                    Value="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                    TickFrequency="{Binding Step}"
                                    SmallChange="{Binding SmallChange}"
                                    IsSnapToTickEnabled="{Binding SnapToTicks}"
                                    Margin="12,0"
                                    VerticalAlignment="Center" />

                            <TextBlock Grid.Column="2"
                                       Text="{Binding ValueString}"
                                       FontSize="13"
                                       Foreground="#FFBBBBBB"
                                       VerticalAlignment="Center"
                                       Margin="12,0,0,0" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <DockPanel Grid.Row="3">
            <StackPanel Orientation="Horizontal">
                <Button Content="開始取像"
                        Width="140"
                        Height="44"
                        Command="{Binding StartPreviewCommand}"
                        Style="{StaticResource ToolbarButton}" />
                <Button Content="停止取像"
                        Width="140"
                        Height="44"
                        Command="{Binding StopPreviewCommand}"
                        Style="{StaticResource ToolbarButton}"
                        Margin="16,0,0,0" />
                <Button Content="單次取像"
                        Width="140"
                        Height="44"
                        Command="{Binding CaptureOnceCommand}"
                        Style="{StaticResource ToolbarButton}"
                        Margin="16,0,0,0" />
            </StackPanel>
            <TextBlock DockPanel.Dock="Right"
                       Text="{Binding StatusMessage}"
                       FontSize="14"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center"
                       Foreground="#FF444444" />
        </DockPanel>
    </Grid>
</Window>
