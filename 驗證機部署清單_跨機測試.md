---
date: 2026-08-18
type: 部署清單（跨機測試前置）
scope: 主程式整併版（站端 App × 中央推論）— 要把哪些東西傳到驗證機
用途: 照這份清單把檔案送到驗證機，就能做 01_測試計畫.md 的 B 類跨機驗證
---

# 驗證機部署清單 — 要傳什麼過去

> **一句話**：驗證機當**站端（產線機）**，只需要傳 **站端程式 + 2 個模型檔 + 測試影像**，
> 然後改 **1 個設定值**（中央推論位址）。**開發機當中央推論機，不用搬。**

---

## 0. 角色怎麼分（這個不能反過來）

| 機器 | 角色 | 跑什麼 | 為什麼 |
|---|---|---|---|
| **開發機**（目前這台） | **中央推論機（父）** | `AIVision.Api` + `AIVisionServerConsole.exe` | **GPU(RTX 3050)、`D:\OCR_demo` 的 CRNN python、lens-gpu 環境都在這台**，搬不走 |
| **驗證機** | **站端（子）** | `AIVision.exe` | 只做前處理與送檢，不需要 GPU |

> ⚠ 反過來（驗證機當父）**行不通** —— 它沒有 CRNN 推論環境。

---

## 1. 要傳到驗證機的東西（共 3 項）

### ① 站端程式（整個資料夾）

| 來源（開發機） | 目的地（驗證機） | 大小 |
|---|---|---|
| `D:\新增資料夾\VISION\AIVision\AIVision\AIVision.Presentation.Wpf\bin\Debug\net8.0-windows\win-x64\` | 任意位置，例如 `C:\AIVision站端\` | **約 376 MB** |

- **整個資料夾複製**，不要只挑 exe。
- 這是 **self-contained 發布**：內含 .NET 執行環境、OpenCV、ONNX Runtime → **驗證機不必安裝 .NET，也不必裝 Python**。
- 主要檔案：`AIVision.exe`、`appsettings.json`、`models.json`、`libs\`(相機 SDK)、大量 `.dll`

### ② 本機辨識模型（2 個檔，**中央掉線時要用**）

| 來源（開發機） | 目的地（驗證機，**路徑要一模一樣**） | 大小 |
|---|---|---|
| `D:\AIVisionModels\v671\mohao.onnx` | `D:\AIVisionModels\v671\mohao.onnx` | 20.4 MB |
| `D:\AIVisionModels\v671\xuehao.onnx` | `D:\AIVisionModels\v671\xuehao.onnx` | 20.4 MB |

- ⚠ **路徑寫死在 `appsettings.json` 的絕對路徑**，所以驗證機要放在**同樣的 `D:\AIVisionModels\v671\`**。
  （驗證機沒有 D 槽的話，改 `appsettings.json` 的 `MoldCodeWarpPolar:MohaoModelPath` / `XuehaoModelPath` 指到實際位置。）
- **不傳會怎樣**：程式照常啟動、送檢也正常（中央有回應時完全不受影響），
  但**中央掉線時本機接管會失敗**（讀值顯示「本機辨識失敗」）→ C1 那項就驗不到。

### ③ 測試影像

| 來源（開發機） | 目的地 | 內容 |
|---|---|---|
| `D:\新增資料夾\父子節點POC\dist\sample_images\` | 任意位置 | **30 張**，檔名帶正解（`M101-01_…` → 應讀出 `M101/01`），方便當場核對 |

---

## 2. 傳過去之後要改的設定（**只有 1 個值**）

編輯驗證機上的 `appsettings.json`（在站端程式資料夾內）：

```jsonc
"InferenceServer": {
  "BaseUrl": "http://localhost:5030",     // ← 改成 http://<開發機IP>:5030
  ...
}
```

- **開發機 IP 怎麼查**：開發機 `ipconfig` → 挑**與驗證機同網段**那個（避開虛擬網卡）。
- 也可以不改檔案，改在程式裡設：登入後 **系統 → API 伺服器設定** 填位址。
- 不改的話會打到驗證機自己的 `localhost:5030`（那裡沒服務）→ **全部走本機接管**。

---

## 3. 不用傳的東西（省得白搬）

| 項目 | 為什麼不用 |
|---|---|
| **.NET 執行環境 / Python** | 站端是 self-contained，全內含 |
| `D:\OCR_demo`（CRNN python） | **只有中央推論機需要**，站端不碰 |
| `AIVision.Api`、`AIVisionServerConsole.exe` | 都是中央推論機的，留在開發機 |
| `D:\AIVisionModels\moldcode_v3_18cls.onnx` | 舊的單 head 路徑，**開發機本身也沒有這個檔**，不影響 |
| 父子節點 POC 的 `dist\`（python 那套） | 已整併進主程式，跨機測試不再需要 |

---

## 4. 怎麼把檔案送過去

| 方式 | 適用 |
|---|---|
| **隨身碟** | 最單純；約 420 MB，壓縮後更小 |
| **HTTP 傳檔** | 開發機跑 `D:\新增資料夾\_share_tmp\送檔給子機_HTTP.bat`，驗證機瀏覽器開 `http://<開發機IP>:8770/` 下載（免帳密） |
| 網路芳鄰 / 遠端桌面複製 | 若兩機已能互通、且你在驗證機前面操作最快 |

---

## 5. 到驗證機後的檢查（3 步，確認搬對了）

```powershell
# ① 站端程式在不在
Test-Path "C:\AIVision站端\AIVision.exe"

# ② 本機模型在不在（中央掉線時要用）
Test-Path "D:\AIVisionModels\v671\mohao.onnx"
Test-Path "D:\AIVisionModels\v671\xuehao.onnx"

# ③ 連得到中央嗎（看 TCP，不要用 ping）
Test-NetConnection <開發機IP> -Port 5030      # 要 TcpTestSucceeded : True
```

三個都 True 就可以開始跑 `doc\包一包\01_測試計畫.md` 的 **B 類跨機驗證**。

---

## 6. 開發機這邊要做的（父端，2 件事）

1. 啟動 **`AIVision.Api`**（中央推論服務，監聽 5030）
2. 啟動 **`AIVisionServerConsole.exe`**（監控畫面，看驗證機送來的狀態）
3. **防火牆放行 5030 入站**（第一次要做）：以系統管理員執行
   ```
   netsh advfirewall firewall add rule name="AIVision Api 5030" dir=in action=allow protocol=TCP localport=5030
   ```

---

## 7. 登入帳號（站端送檢頁需要工程師以上）

| 帳號 | 密碼 | 角色 |
|---|---|---|
| `eng1` | `1234` | 工程師 ← **用這個** |
| `vendor` | `admin888` | 廠商管理員 |

> 帳號定義在 `appsettings.json` 的 `Authentication:Users`，兩台機器各自獨立。

---

## 8. 已知會踩的坑（都踩過了）

| 坑 | 症狀 | 解法 |
|---|---|---|
| 位址沒改 | 全部「本機接管」、送達中央 0 | 改 `BaseUrl` 或用「API 伺服器設定」填開發機 IP |
| 用 ping 判斷連線 | ping 不通就以為網路壞了 | **改用 `Test-NetConnection -Port 5030`**；防火牆常擋 ICMP |
| 防火牆沒開 5030 | TcpTestSucceeded: False | 開發機跑第 6 節的 netsh 指令 |
| 模型沒傳 | 中央掉線時讀值「本機辨識失敗」 | 補傳 `v671\mohao.onnx` / `xuehao.onnx` |
| 首張特別慢（20–90 秒） | 以為當掉 | **正常**，中央 CRNN 冷啟；父端監控會顯示「待機中」→「運作中」 |
| 手機熱點當橋 | 兩機互不相通 | 熱點會隔離裝置，**改用網路線/交換器** |

---

## 附：完整搬運總量

```
站端程式        約 376 MB
本機模型 ×2     約  41 MB
測試影像 30 張  約   3 MB
────────────────────────
合計            約 420 MB
```
