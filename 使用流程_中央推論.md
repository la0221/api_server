# 使用流程：中央推論（API Server）

> 對象：要用「中央伺服器」做辨識測試/試模的工程師。
> 一句話：App 把圖用 HTTP 送到 API server（`localhost:5030`），server 跑雙 head ONNX 回讀值+信心；**生產辨識不受影響**（仍走本機，除非未來明確切換）。
> 主線目標見 `ROADMAP.md`；本檔只講「怎麼操作、怎麼解讀、出錯怎麼辦」。

---

## 0. 架構一眼看

```
App(edge)  --HTTP(ip:port)-->  API server (AIVision.Api, :5030)
   送圖                          跑雙 head warpPolar ONNX
   收 JSON(讀值+信心)  <--        決策不在 server，只回觀測
```

---

## 1. 前置：啟動 API server

**最簡單**：雙擊專案根目錄的 **`啟動API伺服器.bat`**（黑視窗留著=server 活著；關視窗=關 server）。

手動指令（等價）：
```powershell
cd "d:\新增資料夾\VISION\AIVision\AIVision"
dotnet run --project "AIVision.Api\AIVision.Api.csproj" -c Release
```
> 效能量測/正式試模**一律用 `-c Release`**（Debug 慢一倍，數字不可信 —— 2026-07-15 踩過）。

**確認活著**：瀏覽器開 `http://localhost:5030/api/infer/health`，看到：
```json
{"status":"ready","modelLoaded":true,"modelVersion":"baseline", "mohaoClassCount":20,"xuehaoClassCount":18,...}
```
- `ready` = 可推論。`degraded` = server 活著但沒模型 → 去 server 的 `AIVision.Api\appsettings.json` 配 `MoldCodeWarpPolar` 兩個 `.onnx` 路徑。
- 關閉：console `Ctrl+C`。

---

## 2. 功能 A：測試中央推論（驗收按鈕）

**目的**：30 秒證明「路通不通」。**位置**：雙軸模型管理頁 →「推論來源」區的紫色按鈕。

1. （可選）先在「② 測試」選好測試影像資料夾——有選才會順便驗讀值，沒選只驗連線。
2. 按 **[測試中央推論]**。

**結果解讀**：

| 顯示 | 意思 | 下一步 |
|---|---|---|
| ✅ 健康檢查 + ✅ 測試讀值 | **全通**：連線、模型、讀值、延遲都 OK | 可以放心跑批量 |
| ✅ 健康檢查 + ❌ 推論逾時 | 路通，但單張耗時 > `TimeoutMs` | 看 §5 疑難排解「逾時」 |
| ✅ 健康檢查 + ⚠ 這張沒讀到碼 | **路是通的**；「無鏡片/讀不到」是 server 的有效觀測（fail-closed），不是故障 | 換張圖再試 |
| ⚠ server 活著但未載入模型 | server 沒配模型 | server appsettings 配 `MoldCodeWarpPolar` |
| ❌ 連不上 server | server 沒開 / 位址錯 / 防火牆 | 先做 §1；再看 §5 |

---

## 3. 功能 B：批量試模（來源＝中央伺服器）★主打

**目的**：整批拍好的圖走 server 出**準確率報告** —— 隔離試模：**不需要、也不會動到本地模型**（線上模型還沒下載也能測）。

1. 「推論來源」選 **◉ 中央伺服器**。
2. 「② 測試」→ 選測試影像資料夾。**正解靠資料夾結構**：
   ```
   <模號>\<穴號>\*.jpg     例：M83\01\xxx.jpg、M101\07\yyy.png
   ```
   選到「模號那層」（如 `...\M83`），並勾「**子資料夾名當穴號正解**」→ 報告會算命中率；不勾或無子資料夾 = 只辨識不計分。
3. 按 **[執行批量辨識]**（會先自動健檢，連不上/沒模型就不開跑）。
4. 看底部**準確率報告**：
   ```
   來源=中央伺服器(baseline)　張數=180　比對=180　模號正確=178 (98.89%)　
   穴號正確=180 (100.00%)　雙軸皆對=178 (98.89%)　|　
   單張來回 p50=402ms p95=463ms（server 推論 p50=320ms p95=326ms）
   ```
   - **來回 vs server 推論**的差 = 網路+HTTP 開銷。
   - 「來源=中央伺服器(**baseline**)」括號裡是 **server 的模型版本**，與本地載入的版本無關。

**規則**：
- 中央來源時「套用相機ROI」自動停用（server 收已裁圖）。
- server **連續 3 次傳輸失敗自動中止**（避免整批慢磨）；「沒讀到」不算失敗、照常記錄。
- 結果表雙擊某列開「辨識過程」視覺化 —— 那是**本機**用同組參數重現的過程圖（僅目視參考），**讀值一律以 server 回應為準**。

## 4. 功能 C：本機批量（原有功能）

「推論來源」選 **◉ 本機 ONNX**（預設）→ 需先「①版本→載入為目前模型」。「套用相機ROI」只在此來源有效：**離線已裁圖不勾**（預設）、全幅相機原圖才勾。

---

## 5. 疑難排解

| 症狀 | 原因 | 解法 |
|---|---|---|
| ❌ 連不上 server | server 沒開／`BaseUrl` 錯／防火牆 | 照 §1 啟動；對 App `appsettings.json` 的 `InferenceServer:BaseUrl`；跨機時開 5030 進站規則 |
| ⚠ degraded／未載入模型 | server 沒配模型 | server `appsettings.json` → `MoldCodeWarpPolar` 配 `mohao/xuehao .onnx` 實體路徑後重啟 |
| ❌ **推論逾時**（健檢卻 ✅） | 單張耗時 > `TimeoutMs`。server 端 Passes=2 單張約 **385ms**（p90 409ms）、**冷啟首張可達 1.1s** —— 逾時設 350 會**必逾時**（2026-07-24 實際踩過，已把試模逾時調成 2000） | App `appsettings.json` → `InferenceServer:TimeoutMs`（試模建議 ≥2000）。⚠ 未來接**生產實時**要另用 < 節拍的緊逾時，勿共用 |
| 大量「無鏡片/讀取失敗」 | 圖不是「已裁判定區域」（送了全幅原圖）／圖質異常 | 中央來源請送已裁圖；全幅圖目前只支援本機來源+勾ROI |
| 模號讀錯但信心低（如 M83 讀成 M58 conf 0.596） | 模型在該模號上弱（**不是**字集問題——v671 含 20 類：M101/M15/M17/M23/M28/M49/M50/M52/M53/M54/M56/M58/M59/M60/M61/M82/M83/M95/M96/NG） | 跑整個資料夾看整體命中率再下結論；生產端有門檻保護（模號 0.60／穴號 0.85，低於即不採信 → fail-closed） |
| 改了 `appsettings.json` 沒生效 | App 讀的是 **bin 輸出目錄**那份；改原始檔要重 build，且 App 要重啟 | 改完 → `dotnet build` → 重開 App |

---

## 6. 設定速查

**App 端** `AIVision.Presentation.Wpf\appsettings.json`：
```json
"InferenceServer": {
  "BaseUrl": "http://localhost:5030",   // server 位址（跨機改成該機 IP）
  "TimeoutMs": 2000,                     // 試模逾時；生產實時另議（<節拍）
  "HealthTimeoutMs": 1000,
  "Enabled": false                       // false=生產辨識照舊走本機（現階段固定）
}
```

**Server 端** `AIVision.Api\appsettings.json`：
```json
"MoldCodeWarpPolar": {
  "MohaoModelPath": "D:\\AIVisionModels\\v671\\mohao.onnx",
  "XuehaoModelPath": "D:\\AIVisionModels\\v671\\xuehao.onnx",
  "Passes": 2,          // 2=與生產一致(~385ms/張)；1=快一倍(~191ms)但尚未大樣本驗證
  "Preprocess": { ... } // 與訓練對齊；Roi 刻意=0（收已裁圖）
}
```

## 7. 相關文件
- 主線目標：`ROADMAP.md`（根目錄）
- API 契約：`.ai\designs\2026-07-14_api_infer_pair_contract.md`
- 整合設計（階段 0-4）：`.ai\designs\2026-07-15_edge_server_integration.md`
- 交接：`.ai\HANDOFF_API.md`
